*.conf
# Note: *.yaml is intentionally NOT ignored — pnpm-lock.yaml must be tracked.
# Re-include deploy/ — the nginx cutover configs (deploy/nginx-*.conf) live
# there and must be tracked, despite the *.conf rule above.
!deploy/**
*.env
*.key
*.pem
node_modules/

# Next.js
.next/
out/
build/
dist/
next-env.d.ts

# Testing
coverage/
playwright-report/
test-results/

# OS / editor
.DS_Store
*.log
.vscode/
.idea/

# Build artifacts
*.tsbuildinfo
tsconfig.tsbuildinfo
.playwright/

# AI agent context — working notes for local AI sessions, not for the
# public repo. The skill definitions under .agents/skills/ (~2.2 MB) and
# the project-specific AI guidance in AGENTS.md are intentionally kept
# out of version control. The condensed public-facing architecture doc
# lives in README.md.
AGENTS.md
.agents/
README-NEXT.md

# Admin : jamais dans le repo public
admin.html

# Local development helpers and manual validation notes
scripts/
MANUAL-VALIDATION.md
